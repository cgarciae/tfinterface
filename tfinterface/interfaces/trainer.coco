from abc import ABCMeta, abstractmethod
from tfinterface.utils import TENSOR, MODEL, ENV

class TrainerInterface(object):
    __metaclass__ = ABCMeta

    model = MODEL

    @abstractmethod
    def fit(self, *args, **kwargs): pass

